<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Agrega botón "Etiquetas (Paginación)" en el header del formulario de picking,
             junto al botón existente de etiquetas de Odoo. -->
        <record id="view_picking_form_label_pagination" model="ir.ui.view">
            <field name="name">stock.picking.form.label.pagination</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='do_print_picking']" position="after">
                    <button name="action_open_label_pagination_wizard"
                            string="Etiquetas (Paginación)"
                            type="object"
                            class="btn-secondary"
                            invisible="state == 'cancel'"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>
